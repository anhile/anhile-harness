{
  "//": [
    "Versions for the packages the generator writes into a new project and this",
    "repository does not itself use: the NestJS API and the React page. pg and its",
    "types are devDependencies here since migrate.mjs is type-checked against them.",
    "In link-shortener these came from apps/*/package.json, because the product",
    "happened to contain one of everything. This repository is the harness alone,",
    "so they are recorded here, once, at the versions the templates were written against.",
    "",
    "Only what no package.json here can give a version for. A name that is also",
    "in package.json is refused, by the resolver and by harness-package.spec.ts:",
    "two copies of a version drift, which is why harness.manifest.json records",
    "packages and never versions. Bump a version here only after generating a",
    "project with it and watching that project's gate go green."
  ],
  "versions": {
    "@nestjs/common": "^11.1.6",
    "@nestjs/core": "^11.1.6",
    "@nestjs/platform-express": "^11.1.6",
    "@nestjs/testing": "^11.1.6",
    "@radix-ui/react-slot": "^1.3.3",
    "@tailwindcss/vite": "^4.3.3",
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^16.3.0",
    "@types/react": "^19.1.10",
    "@types/react-dom": "^19.1.7",
    "@vitejs/plugin-react": "^5.0.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "jest-environment-jsdom": "^30.0.5",
    "lucide-react": "^1.46.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "tailwind-merge": "^3.7.0",
    "tailwindcss": "^4.3.3",
    "ts-node": "^10.9.2",
    "vite": "^7.1.3"
  }
}
