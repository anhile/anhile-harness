# Progress

Newest at the bottom. What closed, the evidence, and the reasons — not the diff.
`node scripts/progress.mjs template` prints the shape.

## 2026-09-11 — anhile-harness starts, with the gate already in it

- **Feature**: none yet
- **Result**: passing
- **Verified by**: `./verify.sh`, on the tree the generator wrote
- **Evidence**: the `verify-log.jsonl` line for the first run
- **Contract changes**: none.
- **Notes**:

  Generated by `harness-init.mjs`. The gate is green on an empty project,
  which is the only starting point from which a red gate means something.

## 2026-09-11 — Phase 0: the gate goes green in the harness's own repository

- **Feature**: none closed
- **Result**: passing
- **Verified by**: `./verify.sh` 6/6
- **Evidence**: the `verify-log.jsonl` line for `20260911T184003Z` (pass), and
  the `stale` line before it, which is the red run this entry starts from
- **Contract changes**: none
- **Notes**:

  The seed passed 11 of 18 suites (HANDOVER.md). Two causes, both closed here:

  1. **Six suites did not compile under `noUncheckedIndexedAccess`**, the
     setting the generator hands every new project. Fixed in the suites, not
     the compiler: `feature-list.spec.ts` gets an `at(list, i)` that names the
     missing entry rather than assigning to undefined; the others get the
     one-line narrowing each site needed.
  2. **The generator could not resolve versions for `--api`, `--web` or
     `--database`.** `versionsAvailable()` read `apps/*/package.json`, which a
     product had and this repository does not. `harness.versions.json` now
     holds exactly the names no package.json here can give a version for, at
     the versions the templates were written against in link-shortener; the
     resolver refuses a name recorded in both files, and
     `harness-package.spec.ts` holds the same line on the committed files.

  `harness-package.spec.ts` asserted a `build.mjs` and a `packages/harness`
  that do not exist; rewritten against `npm pack --dry-run --json`, which is
  the tarball rather than a claim about it. That found `.claude/.work-budget.json`
  in the package: session state, now ignored by git and excluded from `files`,
  and the reason the first run of this session was recorded `stale` — the hook
  rewrote it mid-run.

  The manifest named files that never came across — the `configured` suites,
  the whole `product` tier, five documents under `seeded` — and the boundary
  suite refuses a name with no file. Trimmed to what is here, with the
  reasons kept in the comments.

  `docs/INVARIANTS.md` and `CONTRIBUTING.md` exist now, because
  `feature-list.spec.ts` holds the documented retraction rule to the enforced
  one, and because the shipped scripts cite I8, I11, I12 and I15 by number.
  The numbers are link-shortener's, kept so every citation stays true; the
  gaps are listed as retired.

  Three cases were rewritten rather than fixed, each with the reason in place:
  the config suite's `git log -S` proof, which asked for a commit this
  history was seeded after; its probe-path case, now held to
  `tsconfig.build.json` references rather than to `apps/`; and its migrations
  case, conditional on `database.required`.

  Pushed nothing to `main`: the seed commit has no attested run and the
  pre-push hook refuses it, correctly. `git push --no-verify origin main` is a
  person's overrule, once, for the commit that predates the workflow.
