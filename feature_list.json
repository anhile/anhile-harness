[
  {
    "id": 0,
    "category": "generator",
    "description": "A new project's gate carries only steps it can pass on the first run; every deferred step is named in its AGENTS.md with the line to add",
    "steps": [
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/harness-init.spec.ts — \"the steps a project gets\", \"the gate it writes\" and \"tells the new project which steps are missing\" pass",
      "node bin/harness.mjs init --yes --name probe --database --api --web --into /tmp/probe — the output names three deferred steps and six in the gate",
      "the generate job in CI: five variants scaffolded, installed and gated on every push, all green on the pull request that closes this entry"
    ],
    "passes": true,
    "spec": "specs/2026-09-harness-guarantees.md"
  },
  {
    "id": 1,
    "category": "generator",
    "description": "Every package the manifest names resolves to a version from package.json or harness.versions.json, and no name has a version in both",
    "steps": [
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/harness-package.spec.ts — \"the versions it can give a new project\" passes: every name resolves, no overlap, nothing the manifest does not name",
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/harness-init.spec.ts — \"refuses to resolve a package this repository does not have\" and \"takes the app versions from harness.versions.json\" pass"
    ],
    "passes": true,
    "spec": "specs/2026-09-harness-guarantees.md"
  },
  {
    "id": 2,
    "category": "package",
    "description": "The tarball carries every file the manifest says travels and no guard suite, no workflow of this repository alone, and nothing git ignores",
    "steps": [
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/harness-package.spec.ts — \"what the tarball carries\" passes, asked of npm pack --dry-run --json",
      "npm pack --dry-run — no path under scripts/__tests__, no generate.yml or release.yml, no .work-budget.json"
    ],
    "passes": true,
    "spec": "specs/2026-09-harness-guarantees.md"
  },
  {
    "id": 3,
    "category": "travel",
    "description": "A copied core script contains none of the identifiers of the product the harness was written in",
    "steps": [
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/harness-boundary.spec.ts — \"core names nothing about this product\" passes for every core script, and the word list is proved live against the manifest"
    ],
    "passes": true,
    "spec": "specs/2026-09-harness-guarantees.md"
  },
  {
    "id": 4,
    "category": "gate",
    "description": "A commit on a tree no recorded green run covers, or after a session edit to a protected file, is refused",
    "steps": [
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/commit-gate.spec.ts — every case passes: no receipt, a failed receipt, a receipt for another tree, each shape of git commit, each protected path, the interpreter heredoc",
      "in a session: edit a file, run git commit without ./verify.sh — the hook prints BLOCKED and cites I11"
    ],
    "passes": true,
    "spec": "specs/2026-09-harness-guarantees.md"
  },
  {
    "id": 5,
    "category": "record",
    "description": "feature_list.json accepts only its four legal edits and one closure per commit, and verify-log.jsonl keeps every line, red runs included",
    "steps": [
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/feature-list.spec.ts — every adversarial case passes",
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/verify-log.spec.ts — deletions, rewrites, an emptied file, malformed lines and a backdated run are each refused"
    ],
    "passes": true,
    "spec": "specs/2026-09-harness-guarantees.md",
    "retracted": {
      "spec": "specs/2026-09-verify-log-files.md",
      "reason": "the record it names, verify-log.jsonl, is a directory of one file per run since 2026-09-12; the guarantee moves to #7, worded for the directory",
      "supersededBy": 7
    }
  },
  {
    "id": 6,
    "category": "scripts",
    "description": "Every script the harness ships is type-checked as it runs, under tsc -b with no build step, here and in a generated project",
    "steps": [
      "./verify.sh — step 02 exits 0 with tsconfig.scripts.json referenced from tsconfig.build.json and every scripts/*.mjs and bin/*.mjs carrying // @ts-check",
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/harness-init.spec.ts — \"typechecks the copied scripts, so their @ts-check is not decoration\" passes"
    ],
    "passes": true,
    "spec": "specs/2026-09-harness-guarantees.md"
  },
  {
    "id": 7,
    "category": "record",
    "description": "feature_list.json accepts only its four legal edits and one closure per commit, and verify-log/ keeps every run as a file that is never edited or removed, red runs included, with no conflict between branches",
    "steps": [
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/feature-list.spec.ts — every adversarial case passes",
      "pnpm exec jest --config jest.config.cjs scripts/__tests__/verify-log.spec.ts — a removed, rewritten, emptied, malformed or stray run is refused, and two branches that both recorded runs merge without a conflict"
    ],
    "passes": true,
    "spec": "specs/2026-09-verify-log-files.md"
  },
  {
    "id": 8,
    "category": "harness",
    "description": "Every audit verdict is kept under audit-log/, one tracked file per audit, never edited or removed; a commit that closes an entry carries a READY audit of its own tree under that entry's contract, and CI refuses one that does not",
    "steps": [
      "run /verify-task <spec> on a tree; a file named after the moment appears under audit-log/ with the verdict, the contract and the tree hash, whatever the verdict was",
      "edit or remove a file under audit-log/, or put a file there that is not an audit; the commit gate refuses the commit and step 07's neighbour, audit-log.mjs check, names the file",
      "commit a flip of passes to true without the audit file, then run check-feature-list.mjs --at <that commit> --base <its parent>; it refuses, naming the entry and the missing audit",
      "the same with the READY audit of that tree in the commit; it passes; with a NOT_READY audit, or an audit of another tree or another contract, it refuses",
      "audit-log.mjs tree HEAD equals verify-receipt.mjs hash on a clean checkout, so the recomputation CI relies on is the one the receipt made"
    ],
    "passes": true,
    "spec": "specs/2026-09-audit-log.md"
  },
  {
    "id": 9,
    "category": "harness",
    "description": "A journal entry's Evidence names a run under verify-log/ and, when the entry closed a feature, the READY audit under audit-log/; the stop hook, step 03 and CI's walk refuse an entry a reader cannot follow",
    "steps": [
      "write an entry whose Evidence says \"the log\" and run node scripts/progress.mjs check; it refuses, naming the entry",
      "name a verify-log/<id> that exists; it passes; name one that does not; it refuses with the id",
      "write closed #n in Feature with no audit-log/<id>, or one that said NOT_READY; it refuses; name the READY one; it passes",
      "commit an entry and run progress.mjs check --at HEAD --base HEAD^; the commit's own journal and records are what is read",
      "end a session that committed with such an entry; the stop hook refuses once with the two tail commands that print the ids"
    ],
    "passes": true,
    "spec": "specs/2026-09-journal-pointers.md"
  },
  {
    "id": 10,
    "category": "harness",
    "description": "A branch under spike/ is asked for no receipt at commit time and no journal entry at session end, is refused as a pull request by check-pr-ready and by CI's attest job, and keeps the protected files protected; the generator ships the rule",
    "steps": [
      "git checkout -b spike/anything, edit, stage; a commit goes through with no receipt and stderr says the branch is a spike",
      "edit verify.sh on the spike; the protected-file rule refuses as on any branch",
      "end a session that committed on the spike with no PROGRESS.md entry; the stop hook is silent",
      "start a session on the spike; the ritual prints what a spike is under the branch line",
      "node scripts/check-pr-ready.mjs on the spike refuses with the sentence; node scripts/spike.mjs check --branch spike/x exits 3 with the same one",
      "node scripts/harness-init.mjs --yes into a folder; scripts/spike.mjs is copied and AGENTS.md names the rule"
    ],
    "passes": true,
    "spec": "specs/2026-09-spike-branches.md"
  },
  {
    "id": 11,
    "category": "harness",
    "description": "A feature closes in one commit: an entry may be appended already passing as that commit's one closure, asked for its READY audit by the gate and by CI's walk; PROGRESS.md is outside the tree hash, and the gate reads the journal itself before a commit",
    "steps": [
      "append an entry with passes: true and run node scripts/check-feature-list.mjs; it passes, noting the entry as the commit's closure; append two, or one beside a flip; it refuses",
      "stage that entry with a green receipt and no audit; the commit gate refuses naming the entry; write the READY audit; it lets the commit through",
      "commit it and run check-feature-list.mjs --at HEAD --base HEAD^; without the audit in the commit it refuses, with it it passes",
      "run ./verify.sh, then edit PROGRESS.md; node scripts/verify-receipt.mjs hash is unchanged and the gate does not ask for a new run",
      "write an entry whose Evidence says \"the log\" and stage a commit; the gate refuses citing I12 and the entry; name the run; it passes",
      "do all of it in one commit — contract, entry born passing, change, run, audit, journal entry naming both — and push; CI's walk accepts the commit"
    ],
    "passes": true,
    "spec": "specs/2026-09-one-commit-closure.md"
  },
  {
    "id": 12,
    "category": "harness",
    "description": "A generated web project starts from a design base: tokens in index.css, a catalog of primitives under components/ui, a brief in apps/web/DESIGN.md, and a lint rule that refuses inline styles, arbitrary class values and Radix imports outside the catalog",
    "steps": [
      "node scripts/harness-init.mjs --yes --web into a folder; apps/web/DESIGN.md, src/index.css with @theme, src/components/ui/{button,card,input,label}.tsx and ui.spec.tsx are there, Home.tsx composes from the card",
      "read the project's eslint.config.mjs; the catalog rule is there for --web and absent without it; AGENTS.md names the brief",
      "lint a page with style={{}}, w-[13px], bg-[#123456] or an @radix-ui import under apps/web/src; each is refused with a message naming DESIGN.md; the same inside components/ui is allowed",
      "pnpm install and run the generated project's own gate; it is green, the primitives counted by coverage through ui.spec.tsx"
    ],
    "passes": true,
    "spec": "specs/2026-09-design-base.md"
  },
  {
    "id": 13,
    "category": "harness",
    "description": "The review brief says whether the UI changed and whether a walk is on this machine; review-pr dispatches design-review, an agent that holds a screen to apps/web/DESIGN.md on the walk's screenshots and reports NO WALK when there is none; a --web project points the brief at apps/web/src",
    "steps": [
      "node scripts/pr-review-brief.mjs on a branch that changed nothing under ui.paths; the UI line says unchanged; on one that did, it names the prefix and the newest .generated/ui/<ts>/ or says there is none",
      "read .claude/agents/design-review.md; no walk, no review; a finding names a screenshot, a file and line, a rule, a consequence",
      "read .claude/skills/review-pr/SKILL.md; one of five reviewers, dispatched when the brief says the UI changed",
      "generate a --web project; harness.config.json has ui.paths apps/web/src/; without --web it is empty; a malformed ui.paths is refused"
    ],
    "passes": true,
    "spec": "specs/2026-09-design-review.md"
  },
  {
    "id": 14,
    "category": "harness",
    "description": "./verify.sh --quick [--base <ref>] runs lint, types, the suites affected since the base and the guards, leaves api-e2e, browser-e2e and coverage to the full gate, and is recorded as quick; the commit gate takes it for a commit that closes nothing, and a closure, the audit and CI's walk ask the full gate",
    "steps": [
      "./verify.sh --quick on a branch; the summary says mode: quick with the base, steps 04, 05 and 08 are SKIP, step 03 ran the affected suites; the receipt and verify-log/<id>.json say mode quick",
      "git commit of a change that closes nothing; the gate lets it through and says the run was quick on stderr",
      "stage a closure on the same receipt; the gate refuses naming the entry and ./verify.sh; node scripts/audit-receipt.mjs write refuses too",
      "./verify.sh; the receipt says mode full and the closure goes through with its audit",
      "generate a project; its verify.sh has run_step 03 unit unit_suites and its AGENTS.md names --quick"
    ],
    "passes": true,
    "spec": "specs/2026-09-quick-gate.md"
  }
]
